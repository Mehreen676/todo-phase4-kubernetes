apiVersion: v1
kind: Service
metadata:
  name: todo-chatbot-backend-service
  labels:
    app: todo-chatbot
    component: backend
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
  selector:
    app: todo-chatbot
    component: backend